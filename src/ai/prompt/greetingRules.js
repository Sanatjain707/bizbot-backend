export function greetingRules(business) {
  return `- FIRST message / greeting (Hi/Hello/Hey): a short warm line including the business name — "Namaste! 🙏 ${business.name} mein aapka swagat hai." — then the bulleted services list, then ask which service. This is the ONLY message where the full service list appears; subsequent replies must refer only to the relevant service.`
}
