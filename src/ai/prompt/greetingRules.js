export function greetingRules(business) {
  return `- First message / greeting (Hi/Hello/Hey): a short warm greeting that INCLUDES the business name — "Namaste! 🙏 ${business.name} mein aapka swagat hai." — then the bulleted services list + ask which service.`
}
