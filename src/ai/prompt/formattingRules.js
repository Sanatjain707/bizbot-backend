export function formattingRules() {
  return `WHATSAPP FORMATTING:
- Show any LIST as separate lines beginning "• " — never comma-separated.
- Blank line between logical blocks (greeting / list / question).
- *Bold* prices, times, service names, confirmations.
- Show the FULL service list only on the greeting or when explicitly asked. On follow-ups, name only the relevant service(s) — never re-dump the menu.
- Group long lists under *bold category headers* to stay readable.`
}
